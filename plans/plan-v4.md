# Plan

Principles: Read the plan carefully. You are encouraged to raise questions about typos, ambiguities, uncertainties, etc. Ask to correct obvious typos (with minimal changes) in this document before you code.

## New data generation

I want to revisit the data generation pipeline. So far, our analysis has focused mostly on two NIAH tasks: argmax and count_avg. I want to add another NIAH task that centers on *counting*. The count_avg already addresses counting to certain degree, but I want to strip away the average calculation part as the model is prone to calculation mistakes.

I'll call the two new NIAH tasks "match_count" and "literal_count". The two tasks follow most of the logic for generating previous dynamic datasets. You will need to probably revise the scripts `generate_dynamic_niah_v2.py` and dependent scripts, and create a new notebook `notebooks/counting_analysis.ipynb`. I will conduct experiments on Google Colab by running the notebook.

- (Optional) prepare haystack. I want to generate a haystack in the same way as before. You can check code block 4 of the notebook `analysis_hidden_states_v4.ipynb` where I called `!PYTHONPATH=src python scripts/gather_paul_graham_essays_v2.py --out-dir data/haystacks/paul_graham` to prepare the haystack. If the haystack already exists (i.e., there are more than 10 txt files with min 5 KB size), then skip this step. Otherwise, you can directly copy the command in the new notebook `notebooks/counting_analysis.ipynb`.

### The "match_count" task

1. Generate the dataset. You can reuse most of the haystack and dataset generation in code block 4 and 5 of the notebook `analysis_hidden_states_v4.ipynb`. However, you need to change the query. Instead of asking for the argmax city and score, the query now asks for the number of cities that receives a score. You need to change the prompt/dataset construction to reflect the changes, including at least the following:
   - The schema was `{\"city\":\"\",\"score\":0}`; now you need to change it so it only contains the *count* (which is expected to be an integer)
   - The gold answer should also change. I think the gold answer should be simply the number of needles inserted, assuming that the haystack does not contain a city with a score (please check)
   - The prompt construction is probably going to be changed, since the schema is now different
2. The control switch. Previously, the  `control_switch` config often has a single `true` value; it decides which needles to be replaced by random haystack tokens. Now I will likely have `control_switch` with all possible true/false values; in other words, `control_switch` may contain no true value, or all true values. Please check carefully whether the current logic implicitly assumes a single true value, and if so, fix it to allow more flexibility.
3. Scoring mechanism. Since the query is changed, you also need to change the scoring mechanism. It is likely easier to verify since we have a single integer in the scheme to verify against.
4. Generate responses and metrics. You can reuse most of the stuff in code block 6 of the notebook `analysis_hidden_states_v4.ipynb`. Make this part optional in the notebook as in `analysis_hidden_states_v4.ipynb`. Add a global boolean variable `RUN_GENERATION_EVAL` in the early part of the new notebook to indicate whether to run this block.
5. Run hidden-states analysis. I think that you can also reuse most stuff in code block 7 of the notebook `analysis_hidden_states_v4.ipynb`. Similarly we expect figures to be generated by running this code block.
6. The output workflow. You should follow the logic in `notebooks/single-example-v2.ipynb` when generating output files. Namely, save generated outputs to `/content/{run_name}` while running, delete large .pt files exceeding 100 MB (if they exist), zip all generated files, move to the specified output folder in the Google Drive, and finally have an optional "Release the Colab runtime code block".

### The "literal_count" task

1. Generate needles. For each example, randomly generate a canary string (namely UID), which are a sequence of letters/numbers that look like ID numbers. The length should be 20--40 letters. All the needles will be exactly the same canary string. When inserting the needles into the haystack, please avoid creating different tokenization due to boundary effects. In other words, if the canary string is encoded as a token sequence, then I expect that the inserted text contains an exact copy of the sequence at each needle position.
   - One possible solution is to insert each needle with strong delimiters to avoid incorrectly splitting or merging characters at the boundary
   - To double check, you can tokenize the final full text, and then check that the needle token IDs occur exactly as a contiguous subsequence.
2. Generate the dataset. Similar to the previous section, make corresponding changes to the existing workflow of dataset generation (e.g., gold answers, prompt construction). For this "literal_count" task, the query should ask for the number of exact copy inside the quotation marks "needle" (where you replace the needle with the generated canary string).
3. The control switch. The same as the previous section.
4. Make corresponding changes to scoring mechanism.
5. "Generate responses and metrics", "Run hidden-states analysis", "The output workflow" parts should be the same as in the previous section, I think.


**Revision.** For hidden-state analysis, keep the existing assumption that exactly one value in `control_switch` is `true`. Do not generalize the hidden-state analysis code to support zero, multiple, or all controlled needles. The counting-analysis notebook should therefore use exactly one controlled needle in the hidden-state analysis section. Dataset generation and response evaluation may still be tested with other `control_switch` settings, but hidden-state analysis should retain the existing one-control workflow.

**Revision.**
- Keep haystack sampling and needle generation controlled by separate random seeds. If the haystack random seed is given by the user, haystack text selection/windowing should be deterministic across examples. If the needle random seed is given by the user, generated needles should be deterministic across examples. Check the existing seed logic and revise the pipeline if needed to implement this targeted behavior.
- Allow `insertion_positions` to contain `null` values. For example, `[null, 200, 400]` and `[null, 200, null]` still indicate three generated needles, but only needles whose aligned insertion position is not `null` are inserted into the haystack. This makes it easier to compare generated datasets with different insertion-position configs while using the same random seeds and the same total number of generated needles.
- Write a concise text description in the new notebook, and add a clear description of the new counting tasks and relevant configuration behavior in the updated `README.md`.

## Ablation analysis

The main goal is to develop ablation analysis for the two new tasks similar to the earlier `argmax` and `count_avg` tasks. You are going to write main python scripts under the new folder `src/counting`, and expand the notebook `counting_analysis.ipynb`. You will likely reuse or refactor existing files / code, including
- `src/single_example/ablation_analysis.py`
- `src/single_example/ablation_representation_analysis.py`
- `src/single_example/ablation_representation_analysis_restore.py`
- `notebooks/single-example-v2.ipynb`

The high-level instruction is the following.
- Make extension of the current logic of `counting_analysis.ipynb`, going beyond dataset generation, evaluation, and hidden states analysis, to include additional three-levels of ablation analysis.
- The three levels of ablation analysis includes (i) token-level ablation (controlled by `RUN_ABLATION` in `single-example-v2.ipynb`), (ii) representation-level ablation (controlled by `RUN_REPRESENTATION_ABLATION`), and (iii) representation-level ablation that restores corrupted hidden states (controlled by `RUN_REPRESENTATION_RESTORE`).

Here are detailed instructions.
1. Introduce useful global variables in the notebook. Refer to Block 3 of `single-example-v2.ipynb`.
   - You should keep the global variables in "Token-level ablation settings", "Representation-level ablation settings", and "Representation-level restore settings". You can reuse the config files in the folder `configs` for the ablation analysis.
   - Check that the default is `NUM_EXAMPLES = 20`.
2. Revise the example selection. In `single-example-v2.ipynb`, the test examples are specified by `EXAMPLE_ID = 0` and `ALL_EXAMPLES = False` in the setting. Instead of running the analysis for a single example or for all examples, I want to run the analysis example by example by looping over a set of examples.
   - The set is specified by a new list variable `SELECT_EXAMPLE_ID = [0]`. The user can change the list to run the ablation analysis. For example, the default value `[0]` stills run the single-example analysis; if `SELECT_EXAMPLE_ID = list(range(0,10))`, then the same analysis is applied in a loop from example ID 0 to 9.
   - Please add a comment to explain `SELECT_EXAMPLE_ID` in the notebook, and add a simple validity check to make sure `SELECT_EXAMPLE_ID` is a subset of `list(range(NUM_EXAMPLES//2))`.
   - The existing workflow of data generation, evaluation, and hidden states analysis should remain the same; `SELECT_EXAMPLE_ID` does not affect the existing part. In particular, the evaluation and figures use the first half of examples in the generated dataset. (Please double check.)
3. Revise the name of the run folder. Currently, the name is something like `run_20260609_205951_Qwen_Qwen3-8B_task-match_count_prompt-easier_len-1000_needles-3`. I want to keep the beginning `run_{date}_{time}`, but modify the later part of the string.
   - replace `Qwen_Qwen3-8B` by `Qwen3-8B`. When using an LLM model, you don't need the model family name since it is redundant.
   - remove `task`, `prompt`, and `needles-3`
   - Add the position insertion positions
   - Let me give you an example of target name: `run_20260609_205951_Qwen3-8B_match_count_easier_1000_needles_100_200_400`. If one of the needle position is null, then a possible example is `run_20260609_205951_Qwen3-8B_match_count_easier_1000_needles_100_null_400`
4. When you revise the three-level ablation analysis, think about whether it is a good idea to merge the ablation files:
- `src/single_example/ablation_analysis.py`
- `src/single_example/ablation_representation_analysis.py`
- `src/single_example/ablation_representation_analysis_restore.py`
You can either create three files or a merged file under the new folder `src/counting`.
5. The I/O logic, zipping, and logging policies remain the same.

**Cleanup**. I want to reduce/remove some large generated files after the notebook "counting_analysis.ipynb" completes running. I spotted a few large files: "corrupted_needle_tokens.jsonl", the folder "attention_stats/input_*", and the folder "qk_cache". Please delete these files before zipping. You likely need to revise the code in block 8 where the block `DELETE_LARGE_PT_WHEN_DONE` cleans up large files. Also provide a comment or a print message to concisely explain what are deleted.

## CoT (thinking mode) analysis

Previously, the hidden states analysis and ablation analysis are only for nonthinking mode; in other words, we turned off the model's thinking mode (for Qwen3, there is an argument in the tokenization) and used the prompt to conduct the analysis without generating new tokens.

**Main goal**. In this section, we want to extend the hidden states analysis and ablation analysis to the thinking mode. We let the model generate reasoning tokens until reasoning stops. Then, we apply the previous hidden states analysis and ablation analysis to the expanded sequence (namely, prompt + generated new tokens), with a few modifications.

**Where your key edits are**. You are likely going to add and revise scripts in the folder `src/counting`, revise the notebook `counting_analysis.ipynb`, and update the readme.md.

**Assumption**.
- There should be a global variable `USE_THINKING` in the notebook that overrides the thinking mode related config parameter.
- The Q/K-cache workflow is ready.
- As before, `SELECT_EXAMPLE_ID` in the notebook provides the set of examples to analyze.
- Use greedy decoding (temperature is 0) for generating new tokens.

**The triggering variable**. Introduce a new global variable `ANALYZE_REASONING_TOKENS = False` in the notebook. If false, the workflow is exactly the same as before: keep existing pipeline intact and we shouldn't see any change. If true, it will trigger the new analysis (you need to check that `USE_THINKING = True`, otherwise abort). Add a clear comment to explain `ANALYZE_REASONING_TOKENS` at its definition.
- If `ANALYZE_REASONING_TOKENS = True`, append `_cot` to the run name. For example, we will have `run_20260609_205951_Qwen3-8B_match_count_easier_1000_needles_100_null_400_cot` or `run_20260609_205951_Qwen3-8B_match_count_easier_1000_needles_100_200_400_cot` as the run name.

The instructions below describe the pipeline when `ANALYZE_REASONING_TOKENS = True`.
1. As before, the hidden states analysis applies to all examples. The PCA analysis applies to the testing split (currently first half of all examples). And ablation analyses loop over the set of examples indicated by `SELECT_EXAMPLE_ID`. For example, if `SELECT_EXAMPLE_ID = [0,1,2]`, then the folder `ablation_examples` within the run folder contains three subfolders for respective example id.
2. Revise generation (Block 5 `Optional response generation and metrics` in the notebook `counting_analysis.ipynb`). For each example, first you need to do `model.generate()` to generate reasoning tokens as in common practice.
   - If generation hits the max_new_tokens before generating an EOS token, flag this issue, print it in visible way to warn the user.
   - Locate the token position in the model's generation such that the model is about to start generating the final answer---in other words, the reasoning token right before generating the final answer. I think you can use the schema to identify the right position. For example, if the schema is `{"count":0}`, we expect the final answer to start with a `{` token, so we locate the token right before the `{` token.
   - Note: the previous part is a bit tricky. I suggest that you do careful sequence-token matching or parsing to identify the right location. You can also suggest alternatives.
   - For each example, save the "extended input" (prompt + reasoning before final answer) as `{run_name}/tensors/inputs_cot_{example_id}.pt`. This file will likely be used in the hidden states and ablation analysis (correct me if I am wrong.)
   - Create a json file `{run_name}/tables/cot_info.json`, which gives three values for each example (i) number of tokens for the prompt, (ii) number of tokens for the reasoning tokens before final answer, and total number of new generated tokens (including reasoning tokens, (iii) final answer tokens, and EOS token).
3. Hidden states analysis. Now the hidden states analysis will be applied to the extended input. Remember that we define the "extended input" as the prompt + reasoning tokens before final answer.
   - I think you can reuse most of the existing code. The key difference is that instead of the prompt input, you now calculate hidden states, cosine similarities, and other scores using the extended input.
   - Revise the visualization `{run_name}/figures/inputs_{example_id}`. Please add a dashed vertical line (perhaps via `axvline()`) at the end position of the extended input, namely, the length of the extended input.
4. Ablation analysis. As before, we consider three levels of ablation analysis: token-level, representation-level, and representation-restore.
   - Again, I think you can reuse most of the existing code. Adapt the existing analysis to the extended input instead of the prompt input.
   - Similarly, when you do hidden states and Q/K-cache calculation, you need to use the extended input.
   - Similarly, for critical-token patterns (massive activation tokens, sink tokens, needle sensitive tokens), use the extended input. We should have the same set of patterns as the existing notebook generates.
   - When you evaluate the token-level ablated input, you follow the similar strategy where you use `model.generate()` (double check this, correct me if I am wrong).
   - When you evaluate the two types of representation-level ablated version, you follow a similar logic as before. To generate each new token, you do activation patching at select critical tokens at a specific layer range.
   - Since we continue generation starting from the end of the extended input, we naturally expect that the model will generate the final answer. Please use a small value such as max_new_tokens = 64 specifically for scoring ablated inputs.
   - Score the ablated inputs as before.


