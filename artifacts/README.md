# Local experiment artifacts

- `run_results/`: collected experiment runs from bundle 001.
- `invalid_runs/`: runs classified as invalid by the original workflow.

These complete local outputs are ignored by Git because they are large and generated. Nothing was deleted from either original bundle.
