# Local archive inventory

This directory preserves source material useful for provenance but unsuitable for normal Git tracking.

- `env001/`: retained `.venv` snapshot from bundle 001.
- `environment_snapshots/niah_001_venv/`: first, partial long-path copy.
- `env002/`: retained `.venv` fragment from bundle 002.
- `source_git_metadata/niah_001_git_dir/`: original nested Git metadata.
- `read_only_files/`: files grouped as read-only in bundle 001.
- `source_archives/`: original packaged source archive.

All paths remain locally available and are ignored by Git.
